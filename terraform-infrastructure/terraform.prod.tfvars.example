# Production Environment Configuration (Template)
# This file serves as a template for production variables
# To use: Copy to terraform.prod.tfvars and update values

project_name = "team3-job-app"
environment  = "prod"
location     = "UK South"

tags = {
  environment = "production"
  managed_by  = "terraform"
  cost_center = "operations"
}
